We are using career-ops through Claude Code

Use plugins this way:

- GSD may create or update .planning/* only.
- OrchestKit may inspect, verify, and review.
- ECC may enforce rules/security checks.
- Do not edit AGENTS.md, CLAUDE.md, modes/_shared.md, scripts, or templates unless I explicitly approve.
- User-layer files may be edited: cv.md, article-digest.md, config/profile.yml, modes/_profile.md, portals.yml, data/*, reports/*, output/*, jds/*.

First inspect the repo and report:

1. current plugin commands available,
2. career-ops setup health,
3. missing user-layer files,
4. next safest action.
Do not modify files yet.
